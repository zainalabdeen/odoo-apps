<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="portal_leave_request.list">
      <field name="name">portal_leave_request list</field>
      <field name="model">portal_leave_request.portal_leave_request</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="portal_leave_request.action_window">
      <field name="name">portal_leave_request window</field>
      <field name="res_model">portal_leave_request.portal_leave_request</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="portal_leave_request.action_server">
      <field name="name">portal_leave_request server</field>
      <field name="model_id" ref="model_portal_leave_request_portal_leave_request"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="portal_leave_request" id="portal_leave_request.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="portal_leave_request.menu_1" parent="portal_leave_request.menu_root"/>
    <menuitem name="Menu 2" id="portal_leave_request.menu_2" parent="portal_leave_request.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="portal_leave_request.menu_1_list" parent="portal_leave_request.menu_1"
              action="portal_leave_request.action_window"/>
    <menuitem name="Server to list" id="portal_leave_request" parent="portal_leave_request.menu_2"
              action="portal_leave_request.action_server"/>
    -->
  </data>
</odoo>